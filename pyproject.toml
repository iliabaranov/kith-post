[project]
name = "kith"
version = "1.2.0"
description = "Free, self-hosted, privacy-first digital invitations sent from your own Gmail."
readme = "README.md"
requires-python = ">=3.12"
license = { text = "MIT" }
dependencies = [
    "fastapi>=0.139.1",
    "uvicorn[standard]>=0.51.0",
    "jinja2>=3.1",
    "pydantic-settings>=2.6",
    "sqlalchemy>=2.0",
    "itsdangerous>=2.2",
    "cryptography>=43",
    "google-auth>=2.56.0",
    "google-auth-oauthlib>=1.2",
    "google-api-python-client>=2.140",
    "pillow>=12.3.0",
    "python-multipart>=0.0.12",
    "slowapi>=0.1.9",
]

[dependency-groups]
dev = [
    "pytest>=8.3",
    "httpx>=0.27",
    "ruff>=0.15.21",
    "mypy>=2.3.0",
    "httpx2>=2.7.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["kith"]

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM"]

[tool.ruff.lint.flake8-bugbear]
# FastAPI's idioms: Depends()/Form()/File() in argument defaults are intended.
extend-immutable-calls = ["fastapi.Depends", "fastapi.Form", "fastapi.File"]

[tool.pytest.ini_options]
addopts = "-q"
testpaths = ["tests"]

[tool.mypy]
python_version = "3.12"
ignore_missing_imports = true
